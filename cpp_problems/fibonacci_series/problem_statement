1. Write code in cpp for printing the output of fibonacci series code using recursion
2. Write code in cpp for printing the output of fibonacci series code using iteration
3. Write code in cpp for printing the output of fibonacci series code using memoization

int fib(int n){
    if (n <=1 ){
        return n;
    }else {
        return fib(n-1) + fib(n-1);
    }
}


2

int fix(int n){
    int t0 = 0 , t1 = 1, s = 0 , i = 0;
    for(i = 2; i < n ; i++){
        s = t0+t1;
        t0 = t1;
        t1 = s;
    }
    return s;
}